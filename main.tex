\documentclass{article}
\usepackage[margin=0.5in]{geometry}
\usepackage{graphicx} % Required for inserting images
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{xcolor}
\usepackage{pgfplots}
\usepackage{placeins}
\pgfplotsset{compat=1.18}
\usepgfplotslibrary{groupplots}
\newcommand{\hl}[1]{\colorbox{red}{\ensuremath{#1}}}

\title{scratch}
\author{David Torres}
\date{}

\begin{document}

\maketitle

\section{Definitions}
Polar decomposition is assumed when mapping

$F^T_0$: deformation gradient of original simulation

$F^T_{0_m}$: deformation gradient mapped

$f_{1}$: incremental deformation of the recovered simulation 

$\bar{F}$: Volume averaged tensor
$V$: Element volume

\section{Methodology}

\subsection{1st method}
Here we assume that the non-volume averaged deformation gradient should be mapped
    \begin{align}
        F^T_0 &= \frac{\partial x_0}{\partial X_0}\\
        \bar{J_0} &= \frac{1}{V_0}\int_{\Omega_0}V_0\det{F^T_0}d V\\ 
        \bar{F}^T_0 &= \left(\frac{\bar{J}_0}{\det{F^T_0}}\right)^{\frac{1}{3}}\hl{F^T_0}
    \end{align}
Note that $\bar{F}^T_0$ is what is seen by the constitutive equations.
After mapping, where $F^T_0 \rightarrow F^T_{0_m}$. Volume averaging is reapplied at initialization:
\begin{align}
    \bar{J}_{0_m} &= \frac{1}{V_1}\int_{\Omega_1}V_1\det \hl{F^T_{0_m}} dV\\
    \bar{F}^T_{0_m} &= \left(\frac{\bar{J}_{0_m}}{\det{\hl{F^T_{0_m}}}}\right)^\frac{1}{3}\hl{F^T_{0_m}}
\end{align}

Then the current incremental deformation gradient is solved and averaged, followed by the calculation of the deformation gradient
\begin{align}
    F_1 &= \frac{\partial x_1}{X_0}\\
 \bar{J}_{1} &= \frac{1}{V_1}\int_{\Omega_1}V_1\det F_1 dV\\
    \bar{F_1} &= \left(\frac{\bar{J}_{1}}{\det{F_1}}\right)^\frac{1}{3}F_1
\end{align}
And the totaling of the gradient
\begin{align}
    \bar{F}_1^T = \bar{F}_1\bar{F}_{0_m}
\end{align}
If expanded out,
\begin{align}
    \bar{F}_1^T = \bar{F_1}\left[\left(\left(\frac{1}{V_1}\int_{\Omega_1}V_1\det{F^T_{0_m}}dV\right)\left(\det{F^T_{0_m}}\right)^{-1}\right)^{\frac{1}{3}}F^T_{0_m}\right]
\end{align}

\subsection{Method 2}
In this method, the 0th step deformation gradient is taken as a whole. 
    \begin{align}
        F^T_0 &= \frac{\partial x_0}{\partial X_0}\\
        \bar{J_0} &= \frac{1}{V_0}\int_{\Omega_0}V_0\det{F^T_0}d V\\ 
        \hl{\bar{F}^T_0} &= \left(\frac{\bar{J}_0}{\det{F^T_0}}\right)^{\frac{1}{3}}F^T_0
    \end{align}
There is no initialization step, as the volume average has already been taken. 
\begin{equation}
    \bar{F}^T_{0_m} = \left[\left(\frac{\bar{J}_0}{\det{F^T_0}}\right)^{\frac{1}{3}}F^T_0\right]_m
\end{equation}

The process for taking the deformation gradient of the restarted simulation is the same.
\begin{align}
    F_1 &= \frac{\partial x_1}{X_0}\\
 \bar{J}_{1} &= \frac{1}{V_1}\int_{\Omega_1}V_1\det F_1 dV\\
    \bar{F_1} &= \left(\frac{\bar{J}_{1}}{\det{F_1}}\right)^\frac{1}{3}F_1
\end{align}
Then the total deformation gradient is
\begin{align}
    \bar{F}_1^T = \bar{F}_1\bar{F}_{0_m}
\end{align}
Then expanded,
\begin{align}
    \bar{F}_1^T = \bar{F}_1\left[\left(\frac{\bar{J}_0}{\det{F^T_0}}\right)^{\frac{1}{3}}F^T_0\right]_m
\end{align}
Embedded in $\bar{J_0}$ term is information that pertains to the now non-existent original mesh. 

\FloatBarrier{}
\section{Comparison}

Now we compare the two methods. Figure~\ref{fig:domain} shows the domain, which an $L\times{}L\times L{}$ cube consisting of TET10 elements, where $L=3$. The elasticity model compressible Neo-Hookean hyper-elasticity and the plasticity model is $J_2$ plasticity with Johnson-Cook hardening/softening.

\begin{figure}[htb]
    \centering
    \includegraphics[width=0.25\textwidth]{pictures/mesh.png}
    \caption{Domain.}
    \label{fig:domain}
\end{figure}

The following boundary conditions are applied to force regions of artificial stiffness,

\begin{align*}
    U_y &= t \quad \text{on } \{Y = L\}, \\
    U_y &= 0 \quad \text{on } \{Y = 0\}, \\
    U_x &= 0 \quad \text{on } \{X = 0, Y = L\}, \\ 
    U_z &= 0 \quad \text{on } \{Z = 0, Z = L\}.\\
\end{align*}

The boundary conditions lead a single free surface at $X=L$. The following simulations modify the amount of applied correction varying the poissons ratio, \\$\nu = \{0.3,0.4.0.45,0.48,0.49,0.459\}$, with the most correction occuring at $\nu=0.459$. The difference between the uncorrected and volumetric Cauchy stress is used as the metric for how much correction occurs:
\begin{equation}
    P_{\text{diff}} = K(\bar{J}-J),
\end{equation}
where $K$ is the bulk modulus, J is the Jacobian, and $\bar{J}$ is the volume averaged Jacobian. The relative error is calculated as
\begin{equation}
    \left[\frac{\int_{\Omega_1}\psi^e_1-\int_{\Omega_0}\psi^e_0}{\int_{\Omega_0}\psi^e_0}\right]_{t=t_r+\Delta t},
\end{equation}
where $\psi^e$ is the elastic energy and $t_r$ is the time of recovery. The 0th simulation is ran for an extra step into order to compute fields at $t_r+\Delta t$. The restarted simulation uses the field data at $t=t_r$.

Several different scenarios are considered for the comparison:
\begin{itemize}
    \item Quasi-static + elastic: Figures~\ref{fig:fbar_strength_comparison_elastic_quasi},~\ref{fig:elastic_energy_quasi_elastic}
    \item Dynamic + elastic: Figures:~\ref{fig:fbar_strength_comparison_elastic},\ref{fig:elastic_energy_dynamic_elastic}
    \item Quasi-static + elastic + plastic: Figures~\ref{fig:fbar_strength_comparison_quasi},\ref{fig:elastic_energy_quasi_elastic}
    \item Dynamic + elastic + plastic: Figures~\ref{fig:fbar_strength_comparison},\ref{fig:fbar_strength_comparison}
\end{itemize}

% \input{figures/fbar_comparison}

% \input{figures/fbar_strength}

\input{figures/fbar_strength_comparison_elastic_quasi}
\input{figures/elastic_energy_quasi_elastic}

\input{figures/fbar_strength_comparison_elastic}
\input{figures/elastic_energy_dynamic_elastic}

\input{figures/fbar_strength_comparison_quasi.tex}
\input{figures/elastic_energy_quasi_plastic}

\input{figures/fbar_strength_comparison}
\input{figures/elastic_energy_methods}

\FloatBarrier{}
\subsection{BC sweep}

\input{figures/bc_strength_comparison_quasi_elastic}
\input{figures/bc_elastic_energy_quasi_elastic}

\input{figures/bc_strength_comparison_elastic}
\input{figures/bc_elastic_energy_elastic}

\subsection{Single element test}

\input{figures/single_element/single_element_error_elastic_quasi}

\end{document}
